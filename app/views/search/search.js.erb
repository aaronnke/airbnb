$('#new-container').append('<%= j render @listings %>');
<% unless @listings.next_page.nil? %>
	$('.pagination').replaceWith('<%= j paginate @listings, :params => {:query => @query} %>');
<% else %>
	$(window).off('scroll');
	$('.pagination').remove();
<% end %>